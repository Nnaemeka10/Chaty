import CreateGroupStepper from "./CreateGroupStepper";

const CreateGroupModal = ({ isOpen, onClose }) => {
  return <CreateGroupStepper isOpen={isOpen} onClose={onClose} />;
};

export default CreateGroupModal;
